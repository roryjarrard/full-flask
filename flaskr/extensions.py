# This module holds extension objects that will be initialized in create_app().
# Keeping them here avoids circular imports between the app factory and the
# extensions.

# Example placeholder:
# from flask_sqlalchemy import SQLAlchemy
# db = SQLAlchemy()
